// Feature flags. Flip a value to re-enable a temporarily disabled area.
//
// BREAKDOWNS_ENABLED: gates the /breakdowns index + posts, the nav link, and
// the sitemap entries. Set to `true` to bring the AI-research breakdowns back.
export const BREAKDOWNS_ENABLED: boolean = false;
